import Head from "next/head";
import Layout from "@/components/Layout";

const About = (props) => {
  return (
    <Layout>
      <h1 className='header'>O nama</h1>
    </Layout>
  );
};

export default About;
